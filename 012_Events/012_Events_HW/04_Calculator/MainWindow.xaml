<Window x:Class="Calculator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Calculator"
        mc:Ignorable="d"
        Title="Calculator" Height="160" Width="300"
        ResizeMode="CanMinimize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" Content="x:" 
                   VerticalAlignment="Center" Margin="0,0,2,0" 
                   FontSize="14"/>
            <TextBox Grid.Column="1" 
                     Name="FirstOperandTextBox" Text="0"
                     FontSize="14" Padding="5" Height="30" 
                     VerticalContentAlignment="Center"
                     HorizontalContentAlignment="Right"
                     ToolTip="Enter first number"/>
            <Button Grid.Column="2" 
                    x:Name="AddButton" Content="+"
                    Width="60" Height="30" 
                    Margin="10,0,5,0" FontSize="14"
                    Click="AddButton_Click"
                    ToolTip="Add"
                    Foreground="White" Background="#007BFF"/>
            <Button Grid.Column="3"  
                    x:Name="SubtractButton" Content="-"
                    Width="60" Height="30" FontSize="14"
                    Click="SubtractButton_Click"
                    ToolTip="Subtract"
                    Foreground="White" Background="#007BFF"/>
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" Content="y:" 
                   VerticalAlignment="Center" Margin="0,0,2,0" 
                   FontSize="14"/>
            <TextBox Grid.Column="1" 
                     Name="SecondOperandTextBox" Text="0"
                     FontSize="14" Padding="5" Height="30" 
                     VerticalContentAlignment="Center"
                     HorizontalContentAlignment="Right"
                     ToolTip="Enter second number"/>
            <Button Grid.Column="2" 
                    x:Name="MultiplyButton" Content="*"
                    Width="60" Height="30" 
                    Margin="10,0,5,0" FontSize="14"
                    Click="MultiplyButton_Click"
                    ToolTip="Multiply"
                    Foreground="White" Background="#007BFF"/>
            <Button Grid.Column="3"  
                    x:Name="DivideButton" Content="/"
                    Width="60" Height="30" FontSize="14"
                    Click="DivideButton_Click"
                    ToolTip="Divide"
                    Foreground="White" Background="#007BFF"/>
        </Grid>

        <!-- Result and Error-->
        <TextBlock Grid.Row="2" x:Name="ResultTextBlock"
           Text="Result:"
           FontSize="16"
           FontWeight="Bold"
           Margin="0,7,0,0"
           HorizontalAlignment="Center"/>

        <TextBlock Grid.Row="2" x:Name="ErrorTextBlock"
           Foreground="Red"
           FontSize="16"
           FontWeight="Bold"
           Margin="0,7,0,0"
           HorizontalAlignment="Center"/>
    </Grid>
</Window>
