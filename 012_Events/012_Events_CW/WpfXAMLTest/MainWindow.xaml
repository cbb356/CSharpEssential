<Window x:Class="WpfXAMLTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfXAMLTest"
        mc:Ignorable="d"
        Title="Calculator" 
        Width="450" 
        Height="300"
        ResizeMode="CanMinimize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Input Row 1: X value -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="x:" VerticalAlignment="Center" Margin="0,0,10,0" FontSize="14"/>
            <TextBox Grid.Column="1" Text="3" FontSize="14" Padding="5" Height="30" VerticalContentAlignment="Center"/>
            <Button Grid.Column="2" Content="+" Width="60" Height="30" Margin="10,0,5,0" FontSize="14"/>
            <Button Grid.Column="3" Content="-" Width="60" Height="30" Background="LightGray" FontSize="14"/>
        </Grid>

        <!-- Input Row 2: Y value -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="y:" VerticalAlignment="Center" Margin="0,0,10,0" FontSize="14"/>
            <TextBox Grid.Column="1" Text="5" FontSize="14" Padding="5" Height="30" VerticalContentAlignment="Center"/>
            <Button Grid.Column="2" Content="×" Width="60" Height="30" Margin="10,0,5,0" FontSize="14"/>
            <Button Grid.Column="3" Content="÷" Width="60" Height="30" Background="LightGray" FontSize="14"/>
        </Grid>

        <!-- Result Display -->
        <TextBlock Grid.Row="2" 
                   Text="Result: 15" 
                   FontSize="18" 
                   FontWeight="Bold" 
                   HorizontalAlignment="Center" 
                   Margin="0,10,0,0"/>

    </Grid>
</Window>
