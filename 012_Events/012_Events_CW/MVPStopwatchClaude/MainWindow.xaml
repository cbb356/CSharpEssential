<Window x:Class="MVPStopwatchClaude.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Stopwatch - MVP Pattern" Height="400" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize">

    <Window.Resources>
        <!-- Styles for consistent look -->
        <Style x:Key="TimeDisplayStyle" TargetType="Label">
            <Setter Property="FontFamily" Value="Consolas, Monaco, 'Courier New', monospace"/>
            <Setter Property="FontSize" Value="48"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2E86AB"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Background" Value="#F8F9FA"/>
            <Setter Property="BorderBrush" Value="#DEE2E6"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="10"/>
        </Style>

        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="100"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="StartButtonStyle" BasedOn="{StaticResource ButtonStyle}" TargetType="Button">
            <Setter Property="Background" Value="#28A745"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#1E7E34"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#6C757D"/>
                    <Setter Property="BorderBrush" Value="#545B62"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="StopButtonStyle" BasedOn="{StaticResource ButtonStyle}" TargetType="Button">
            <Setter Property="Background" Value="#DC3545"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#BD2130"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#6C757D"/>
                    <Setter Property="BorderBrush" Value="#545B62"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ResetButtonStyle" BasedOn="{StaticResource ButtonStyle}" TargetType="Button">
            <Setter Property="Background" Value="#FFC107"/>
            <Setter Property="Foreground" Value="#212529"/>
            <Setter Property="BorderBrush" Value="#E0A800"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#6C757D"/>
                    <Setter Property="BorderBrush" Value="#545B62"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Stopwatch Application"
                   FontSize="24" FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="20" Foreground="#495057"/>

        <!-- Time Display -->
        <Label Grid.Row="1" Name="TimeDisplayLabel"
               Content="00:00:00.000"
               Style="{StaticResource TimeDisplayStyle}"
               VerticalAlignment="Center"/>

        <!-- Control Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" 
                    HorizontalAlignment="Center"
                    Margin="20">

            <Button Name="StartButton"
                    Content="Start"
                    Style="{StaticResource StartButtonStyle}"
                    Click="StartButton_Click"/>

            <Button Name="StopButton"
                    Content="Stop"
                    Style="{StaticResource StopButtonStyle}"
                    Click="StopButton_Click"/>

            <Button Name="ResetButton"
                    Content="Reset"
                    Style="{StaticResource ResetButtonStyle}"
                    Click="ResetButton_Click"/>
        </StackPanel>

        <!-- Status Display -->
        <Label Grid.Row="3" Name="StatusLabel"
               Content="Status: Ready"
               HorizontalAlignment="Center"
               FontSize="14"
               Foreground="#6C757D"
               Margin="10"/>

        <!-- MVP Pattern Info --><!--
        <Border Grid.Row="4" Background="#E9ECEF" 
                BorderBrush="#ADB5BD" BorderThickness="1"
                Margin="10,0,10,10">
            <StackPanel Margin="15,10">
                <TextBlock Text="MVP Pattern Implementation"
                           FontWeight="Bold" FontSize="14"
                           Foreground="#495057"/>
                <TextBlock TextWrapping="Wrap" FontSize="12"
                           Foreground="#6C757D">
                    <Run Text="• Model: StopwatchModel (business logic)"/>
                    <LineBreak/>
                    <Run Text="• View: MainWindow (UI implementation)"/>
                    <LineBreak/>
                    <Run Text="• Presenter: StopwatchPresenter (coordination logic)"/>
                </TextBlock>
            </StackPanel>
        </Border>-->
    </Grid>
</Window>